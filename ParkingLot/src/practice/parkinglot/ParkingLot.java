package practice.parkinglot;

import practice.parkinglot.vehicle.Vehicle;

/**
 * Parking lot API. Three main functions: hasSpot(), park(v), leave(v).
 */
public class ParkingLot {

  /**
   * Check if there are spots in the parking lot.
   * @return true if there exists remaining spots.
   */
  public boolean hasSpot() {
    return true;
  }

  /**
   * Try to park vehicle into the parking lot.
   * @param v vehicle to park in to parking lot.
   * @return true if parked successfully.
   */
  public boolean park(Vehicle v) {
    return true;
  }

  /**
   * Try to drive the vehicle leave parking lot.
   * @param v vehicle to leave the parking lot.
   * @return true if left successfully.
   */
  public boolean leave(Vehicle v) {
    return true;
  }
}
